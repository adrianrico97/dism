<div class="container h-100 mt-5">
  <div class="row">
    <div class="col-9">
      <!-- Formulario -->
      <h1 class="text-left"><%= @device.display_name %></h1>
      <h3 class="text-left"><%= @issue.title %></h3>
      <% if @issue.open?%>
        <%= render 'form' %>
      <%else%>
        <%= render 'header_form' %>
      <%end%>
      <!-- Fin formulario -->
    </div>
    <div class="col-3">
      <!-- Menú lateral -->

      <!-- Fin menú lateral -->
    </div>
  </div>
</div>