<div class="container h-100 mt-5">
  <div class="row">
    <div class="col-9">
      <!-- Formulario -->
      <h1 class="text-left"><%= @brand.display_name %></h1>
      <h3 class="text-left"><%= t('models.new') %></h3>
      <%= render 'form' %>
      <!-- Fin formulario -->
    </div>
    <div class="col-3">
      <!-- Menú lateral -->
      <%= go_back_link(brand_path(@brand)) %>
      <!-- Fin menú lateral -->
    </div>
  </div>
</div>